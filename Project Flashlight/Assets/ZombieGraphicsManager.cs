using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Pathfinding;

public class ZombieGraphicsManager : MonoBehaviour
{

    public AIPath path;
    // Start is called before the first frame update
    void Start()
    {
        //path.canMove = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
